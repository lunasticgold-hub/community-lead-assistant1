{
  "name": "community-lead-assistant",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/web run build",
    "lint": "npm --workspace apps/web run lint",
    "test": "node --test tests/*.test.mjs",
    "check:extension": "node --check apps/extension/background.js && node --check apps/extension/content.js && node --check apps/extension/popup/popup.js"
  },
  "workspaces": [
    "apps/web"
  ]
}
